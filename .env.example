# Database
DATABASE_URL=postgres://postgres:postgres@localhost:6432/openrecall

# =============================================================================
# AI Provider Configuration
# =============================================================================
# You can use the same provider for both chat and embeddings (using AI_BASE_URL),
# or configure them separately for different providers.

# Shared defaults (used if specific provider URLs not set)
AI_BASE_URL=http://localhost:11434/v1
AI_MODEL=llama3.2:8b

# -----------------------------------------------------------------------------
# Chat Provider (for summaries, entity extraction, flashcards, chat)
# -----------------------------------------------------------------------------
# CHAT_PROVIDER=local                           # "local" or "openai"
# CHAT_BASE_URL=http://localhost:11434/v1       # Ollama, LM Studio, or OpenAI-compatible
# CHAT_MODEL=llama3.2:8b                        # Model name
# CHAT_API_KEY=                                 # API key (optional for local)

# -----------------------------------------------------------------------------
# Embedding Provider (for vector embeddings)
# -----------------------------------------------------------------------------
# EMBEDDING_PROVIDER=local                      # "local" or "openai"
# EMBEDDING_BASE_URL=http://localhost:11434/v1  # Ollama, LM Studio, or OpenAI-compatible
EMBEDDING_MODEL=nomic-embed-text                # Model name (768 dimensions)
# EMBEDDING_API_KEY=                            # API key (optional for local)

# -----------------------------------------------------------------------------
# Example: Local chat + OpenAI embeddings
# -----------------------------------------------------------------------------
# CHAT_BASE_URL=http://localhost:11434/v1
# CHAT_MODEL=llama3.2:8b
# EMBEDDING_PROVIDER=openai
# EMBEDDING_BASE_URL=https://api.openai.com/v1
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_API_KEY=sk-...

# -----------------------------------------------------------------------------
# Example: All OpenAI
# -----------------------------------------------------------------------------
# CHAT_PROVIDER=openai
# CHAT_BASE_URL=https://api.openai.com/v1
# CHAT_MODEL=gpt-4o-mini
# CHAT_API_KEY=sk-...
# EMBEDDING_PROVIDER=openai
# EMBEDDING_BASE_URL=https://api.openai.com/v1
# EMBEDDING_MODEL=text-embedding-3-small
# EMBEDDING_API_KEY=sk-...

# Legacy (still works for backwards compatibility)
# OPENAI_API_KEY=sk-...
